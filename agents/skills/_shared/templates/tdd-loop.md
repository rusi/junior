# TDD Loop Template

Repeat per behavior slice:

1. Define one behavior from acceptance criteria.
2. Add/update one test that fails for the right reason.
3. Implement minimum code to pass that test.
4. Run targeted tests.
5. Refactor without changing behavior.
6. Re-run targeted tests.

After completing slices, run full required checks.
